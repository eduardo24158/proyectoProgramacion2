<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../.././stylesheets/style.css" />
    <title>Registro</title>
  </head>
  <body>
    <%- include('../../partials/header.ejs'); %>
    <div class="encabezados Centro">
      <a href="/estudiante/session"><button class="btn-3">Inicio de Sesión</button></a>
      <a href="/estudiante/register"><button class="btn-3">Registro</button></a> 
      <a href="/"><button class="btn-3">Inicio</button></a> 
    </div>
    </header>
    <main class="centro centroregistro">
      <div class="tituloDelRegistro">
        <h1>Registrate Aquí!</h1>
      </div>
      <div class="formulario">
        <form action="/estudiante/register" method="post" class="formu">
          <% if (error) { %>
            <p class="pa" style="color: red;"><%= message %></p>
          <% } %>
          <div class="username">
            <input class="inpust" type="text" name="name" pattern="^[a-zA-Záéíóúñ ]+$" title="Ingresa solo letras" required />
            <label class="labe">Nombre</label>
          </div>
          <div class="username">
            <input class="inpust" type="text" name="lastname" pattern="^[a-zA-Záéíóúñ ]+$" title="Ingresa solo letras" required />
            <label class="labe">Apellido</label>
          </div>
          <div class="username">
            <input class="inpust" type="number" name="age" required />
            <label class="labe">Edad</label>
          </div>
          <div class="username">
            <input class="inpust" type="email" name="email" required />
            <label class="labe">Correo</label>
          </div>
          <div class="username">
            <input class="inpust" type="number" name="cedula" oninput="this.value = this.value.replace(/[^0-9]/g, '');" required />
            <label class="labe">Cédula</label>
          </div>
          <div class="username">
            <input class="inpust" type="password" name="password" maxlength="16" required />
            <label class="labe">Contraseña</label>
          </div>
          <div class="username">
            <input class="inpust" type="password" name="confirmPassword" maxlength="16" required />
            <label class="labe">Confirmar Contraseña</label>
          </div>
          <div class="formuAbajo">
            <p class="pa">
              Estoy de acuerdo con los 
              <a href="/TerminosCondiciones" class="a">Términos y Condiciones</a>
            </p>
            <button class="btn_1" type="submit">Registrarse</button>
            <p class="pa">
              <a href="/estudiante/session" class="a">¿Ya tienes una Cuenta?</a>
            </p>
          </div>
        </form>
      </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if(typeof alert != "undefined") { %>
    <script>
      Swal.fire({
        title: "<%= alertTitle %>",
        text: "<%= alertMessage %>",
        icon: "<%= alertIcon %>",
        showConfirmButtom: "<%= showConfirmButtom %>",
        timer: "<%= timer %>",
      }).then(() => {
        window.location = "http://localhost:3000<%= ruta %>";
      });
    </script>
    <% } %> 
    <%- include('../../partials/footer.ejs'); %>
  </body>
</html>